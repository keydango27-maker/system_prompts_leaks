<!-- 简体中文机器翻译；仅供检索、阅读和研究。 -->
<!-- source-file: OpenAI/tool-file_search.md -->
<!-- source-sha256: 1a3e331a4d30a35f0d639d46e3075a05ddb507393df47ac6f2ad018bd136dce8 -->
<!-- 代码块、URL、XML/HTML 标签和部分技术标识保持原样。 -->
<!-- fragment-fallback-pieces: 0 -->

## file_search  

// 用于浏览和打开用户上传的文件的工具。要使用此工具，请将消息的收件人设置为 `to=file_search.msearch`（以使用 msearch 功能）或 `to=file_search.mclick`（以使用 mclick 功能）。  
// 用户上传的部分文档将自动包含在对话中。仅当相关部分不包含满足用户请求的必要信息时才使用此工具。  
// 请提供您答案的引用。  
// 引用msearch结果时，请按照以下格式渲染：`【{message idx}:{search idx}†{source}†{line range}】`。  
// 消息 idx 在工具的消息开头提供，格式如下 `[message idx]`，例如[3]。  
// 搜索索引应该从搜索结果中提取，例如# 指的是第 13 条搜索结果，来自一个名为 "Paris"、ID 为 4f4915f6-2a0b-4eb5-85d1-352e00c125bb 的文档。  
// 应从特定搜索结果中提取行范围。搜索结果中的每一行内容都以行号和句点开头，例如“1.这是第一行”。行范围应采用“L{起始行}-L{结束行}”格式，例如"L1-L5"。  
// 如果支持证据是从第 10 行到第 20 行，那么对于此示例，有效引用将为 ` `。  
// 引用 msearch 的结果时，引用的所有 4 部分都是必需的。  
// 引用mclick结果时，请按照以下格式渲染：`【{message idx}†{source}†{line range}】`。例如，` `。引用 mclick 结果时，所有 3 部分都是必需的。  

命名空间 file_search {  

// 对用户上传的文件或内部知识源发出多个查询并显示结果。  
// 您一次最多可以向 msearch 命令发出五个查询。  
// 但是，只有当用户的问题需要分解/重写以通过有意义的不同查询找到不同的事实时，您才应该提供多个查询。  
// 否则，更愿意提供一个设计良好的查询。避免过于广泛且会返回不相关结果的简短或通用查询。  
// 您应该为混合构建编写良好的查询，包括关键字和上下文  
// 结合关键字和语义搜索的搜索，并从文档中返回块。  
// 编写查询时，必须包含所有实体名称（例如，公司名称、产品名称、  
// 技术或人员）以及每个单独查询中的相关关键字，因为查询// 彼此完全独立执行。  
// {optional_nav_intent_instructions}  
// 您可以使用两个额外的运算符来帮助您制作查询：  
// * “+”运算符（搜索的标准包含运算符），可增强所有检索到的文档  
// 包含前缀术语。要增强短语/单词组，请将它们括在括号中，并以“+”为前缀。例如。 “+（文件服务）”。实体名称（名称  
// 公司/产品/人员/项目）往往很适合这个！不要分解实体名称 - 如果需要，请将它们括在括号中，然后再添加前缀 +。  
// * “--QDF=”运算符用于传达每个查询所需的新鲜度级别。  
// 对于用户的请求，首先考虑新鲜度对于搜索结果的排名有多重要。  
// 在每个查询中包含 QDF (QueryDeservedFreshness) 评级，范围从 --QDF=0 （新鲜度为  
// 不重要）改为--QDF=5（新鲜度很重要）如下：  
// --QDF=0：请求的是 5 年前的历史信息，或者是不变的既定事实（例如地球的半径）。我们应该提供最相关的结果，无论年龄大小，即使它已经有十年了。新鲜内容没有任何提升。  
// --QDF=1：请求寻求通常可以接受的信息，除非它非常过时。提升过去 18 个月的结果。  
// --QDF=2：请求要求的内容通常不会很快改变。提高过去 6 个月的结果。  
// --QDF=3：请求所要求的内容可能会随着时间的推移而改变，因此我们应该提供过去季度/三个月的内容。提升过去 90 天的结果。  
// --QDF=4：请求要求提供最近的信息或可能快速发展的信息。提升过去 60 天的结果。  
// --QDF=5：该请求要求提供最新或最近的信息，因此我们应该从本月开始提供一些内容。增强过去 30 天及更短时间内的结果。  
// 以下是如何使用 msearch 命令的一些示例：  
// 用户：20世纪70年代法国和意大利的GDP是多少？ => {{"queries": ["1970年代+法国GDP --QDF=0", "1970年代+意大利GDP --QDF=0"]}} # 历史查询。请注意，QDF 参数是为每个查询独立指定的，并且实体以 + 为前缀  
// 用户：报告对 MMLU 上的 GPT4 性能有何评价？ => {{"queries": ["+MMLU 基准上的+GPT4 性能 --QDF=1"]}}  
// 用户：如何将客户关系管理系统与第三方电子邮件集成营销工具？ => {{"queries": ["客户管理系统与+电子邮件营销集成--QDF=2"]}}  
// 用户：我们的云存储服务的数据安全和隐私最佳实践是什么？ => {{"queries"：[“+云存储的+安全性和+隐私的最佳实践--QDF=2”]}}  
// 用户：设计团队正在做什么？ => {{"queries": [“+设计团队当前项目 OKR --QDF=3”]}}  
// 用户：John Doe 在做什么？ => {{"queries": ["当前项目任务 +(John Doe) --QDF=3"]}}  
// 用户：Metamoose 已经启动了吗？ => {{"queries": ["+Metamoose --QDF=4 的启动日期"]}}  
// 用户：这周办公室关门吗？ => {{"queries": ["+2024 年 7 月办公室关闭一周 --QDF=5"]}}  

// 请确保在查询中使用 + 运算符以及 QDF 运算符，以帮助检索更多相关结果。  
// 注意：  
// * 在某些情况下，文档中可能包含 file_modified_at 和 file_created_at 时间戳等元数据。当这些可用时，您应该使用它们来帮助了解信息的新鲜度，与很好地满足用户搜索意图所需的新鲜度水平进行比较。  
// * 文档标题也将包含在结果中；您可以使用它们来帮助理解文档中信息的上下文。请务必使用它们来确保您引用的文档未被弃用。  
// * 当未提供QDF参数时，默认值为--QDF=0，这意味着信息的新鲜度将被忽略。  

// 特殊的多语言要求：当用户的问题不是英语时，您必须用英语发出上述查询，并将查询翻译成用户的原始语言。  

// 示例：  
// 用户：김민준 Been 무엇을 하고 있나요? => {{"queries": ["当前项目任务 +(Kim Minjun) --QDF=3", "현재 프로젝트 및 작업 +(김민준) --QDF=3"]}}  
// 用户: オfuィsuは今周闭まっていますか？ => {{"queries": ["+2024年7月办公室关闭周--QDF=5", "+オfuィsu 2024年7月周闭锁--QDF=5"]}}  
// 用户：¿Cuál es el rendimiento del modelo 4o en GPQA? => {{"queries"：[“+(4o 模型) 的 GPQA 结果”、“4o 模型精度 +(GPQA)”、“+(modelo 4o) 的 GPQA 结果”、“4o 模型的精度 +(GPQA)”]}}  

// **重要信息：** 以下是您有权访问并允许搜索的内部检索索引（知识存储）：  
// **recording_knowledge**  
// 其中：  
// - recording_knowledge：所有用户录音的知识存储，包括记录和摘要。仅当用户询问录音、会议、抄本或摘要。除非用户明确请求，否则请避免过度使用 source_filter 来代替 recording_knowledge — 其他来源通常包含更丰富的一般查询信息。  

输入 msearch = (_: {  
查询？：字符串[]，  
意图？：字符串，  
time_frame_filter？：{  
  start_date：字符串；  
  end_date：字符串；  
},  
}) => 任意；  

} // 命名空间 file_search